import {
    UI_LISTVIEW_STATUS
} from '@cdp/ui-listview';

describe('ui-listview spec', () => {

    it('check status', () => {
        expect(UI_LISTVIEW_STATUS).toBe('UNDER CONSTRUCTION');
    });
});
