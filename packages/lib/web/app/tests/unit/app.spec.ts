/*
 eslint-disable
    @typescript-eslint/no-explicit-any
 */

import { STATUS } from '@cdp/app';

describe('app/utils spec', () => {

    it('template', () => {
        expect(STATUS).toBe('TODO');
    });

});
