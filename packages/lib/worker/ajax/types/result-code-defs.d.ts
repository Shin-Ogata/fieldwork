declare namespace CDP_DECLARE {
    /**
     * @en Extends error code definitions.
     * @ja 拡張通エラーコード定義
     */
    enum RESULT_CODE {
        ERROR_AJAX_RESPONSE,
        ERROR_AJAX_TIMEOUT
    }
}
